<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="120dp"
    android:background="@color/white"
    >

    <TextView
        android:id="@+id/header_team"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:letterSpacing="0.03"
        android:lineSpacingExtra="14sp"
        android:textColor="#000000"
        android:textSize="26sp"
        android:fontFamily="@font/nbcsn_frank_bold"
        android:visibility="invisible"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="@id/guideline2"
        tools:text="EAGLES"
    />

    <ImageView
        android:id="@+id/header_peacock"
        android:layout_width="38dp"
        android:layout_height="22dp"
        android:contentDescription="peacock"
        android:src="@drawable/peacock"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="@id/guideline_v3_2"
    />

    <include layout="@layout/teamview_card_constraint_guidelines"/>

</androidx.constraintlayout.widget.ConstraintLayout>